spring:
  config:
    activate:
      on-profile: dev
  devtools:
    add-properties: true

openapi:
  service:
    api-docs: authentication-service
    server: http://localhost:${server.port}
    title: Authentication service
    version: 1.0.0